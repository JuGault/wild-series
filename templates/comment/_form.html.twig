{{ form_start(form, {'attr': {'class': 'offset-3 col-6 commentary p-3'}}) }}
<div class="d-flex flex-column align-items-center">
        <div class="d-flex flex-column col-4">
            {{ form_label(form.rate, 'Note de l\'épisode') }}
            {{ form_widget(form.rate) }}
        </div>
        <div class="d-flex flex-column col-8 ">
            {{ form_label(form.comment, 'Votre commentaire') }}
            {{ form_widget(form.comment) }}
        </div>
</div>
<div class="d-flex justify-content-around">
    <button class="btn btn-outline-primary col-7">{{ button_label|default('Commenter') }}</button>
</div>
{{ form_end(form) }}
