<nav class="navbar navbar-expand-lg navbar-light bg-orange">
    <a class="navbar-brand" href="{{ path('app_index') }}">Wild Series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link active" href="{{ path('wild_index') }}">Accueil</a>
            <select class="nav-item nav-link" id="choix">
                <option value="" selected >Catégories</option>
                {% for nav_category in nav_categories %}
                    <option value="{{ path('wild_show_category', {categoryName: nav_category.name}) }}">{{ nav_category.name }}</option>
                {% endfor %}
            </select>
            <a class="nav-item nav-link" href="#">Acteurs</a>
            <a class="nav-item nav-link" href="#">Favoris</a>
        </div>
    </div>
</nav>
