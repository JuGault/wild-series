<nav class="navbar navbar-expand-lg navbar-light bg-orange">
    <a class="navbar-brand" href="{{ path('app_index') }}">Wild Series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNavAltMarkup">
        <div class="navbar-nav d-flex w-100 justify-content-around">
            <div class="d-flex w-25 justify-content-around">
                <select class="" id="category">
                    <option value="" >Catégories</option>
                    {% for nav_category in nav_categories %}
                        <option value="{{ path('wild_show_category', {categoryName: nav_category.name}) }}" {% if categoryName is defined %}{% if categoryName is same as(nav_category.name) %}selected{% endif %}{% endif %}>{{ nav_category.name }}</option>
                    {% endfor %}
                </select>
                <a class="nav-item nav-link" href="{{ path('wild_index_actor') }}">Acteurs</a>
            </div>
            <a class="nav-item nav-link " href="{{ path('wild_index') }}">Accueil</a>
            <a class="nav-item nav-link " href="{{ path('program_index') }}">Series</a>
            <a class="nav-item nav-link" href="#">Favoris</a>
            {% if app.user %}
                {{ app.user.email }}
                <a class="nav-item nav-link" href="{{ path('app_profile') }}">Mon compte</a>
            {% else %}
                <a class="nav-item nav-link" href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
